{
  "name": "get-keyvault-secret",
  "version": "1.0.0",
  "description": "Github action for Azure Key Vault Operations",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "npx -q --no-install giismudge .*ignore",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Romil Goyal",
  "license": "ISC",
  "devDependencies": {
    "@slidewave/gitignore-include": "^1.0.5",
    "@types/node": "^12.7.8",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "@actions/core": "1.2.6",
    "@actions/exec": "^1.0.1",
    "azure-actions-webclient": "1.1.0"
  },
  "repository": {
    "type": "git",
    "url": "(https://github.com/Azure/get-keyvault-secrets.git)"
  }
}
